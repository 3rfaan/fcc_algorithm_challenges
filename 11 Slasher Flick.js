function slasher(arr, howMany) {
    return arr.slice(howMany);
}

slasher([1, 2, 3], 2);

// SPLICE METHOD
/* function slasher(arr, howMany) {
     arr.splice(0, howMany);
     return arr;
} */
